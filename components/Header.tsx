import {FC} from 'react';
import {Navigate} from "@/components/Naviagete";


export const Header = () => {
   return (
      <header className={'container py-10'}>
         <Navigate/>
      </header>
   );
};